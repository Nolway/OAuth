# Generated by Okty.io
version: '3.6'
services:
    nginx:
        build: docker/nginx
        ports:
            - '8080:80'
        volumes:
            - './:/usr/share/nginx/html'
    composer:
        image: 'composer:1.8'
        command: install
        volumes:
            - './:/app'
    php-fpm:
        build: docker/php
        volumes:
            - './:/usr/share/nginx/html'
